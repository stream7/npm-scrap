#!/usr/bin/env node
var program = require('commander');
var version = require('./../package').version;
var cli = require('./../cli');

program.version(version);

program
    .arguments('<terms...>')
    .description('Scrap npmjs.com and return search results')
    .parse(process.argv);

cli(program.args, program);
